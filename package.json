{
  "name": "paint-fill",
  "version": "1.0.0",
  "description": "Paint fill effect using flood fill algorithm",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -e src/index.js -o bundle.js "
  },
  "author": "andrew.kralovec",
  "license": "MIT",
  "dependencies": {
    "browserify": "^16.2.3",
    "jquery": "^3.4.0"
  },
  "devDependencies": {
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3",
    "babelify": "^10.0.0"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
