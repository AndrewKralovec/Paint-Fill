{
  "name": "paint-fill",
  "version": "1.0.0",
  "description": "Paint fill effect using flood fill algorithm",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:dir": "browserify -e static/main.js -o public/javascripts/bundle.min.js",
    "build": "browserify index.js -o bundle.js -t"
  },
  "author": "andrew.kralovec",
  "license": "MIT",
  "dependencies": {
    "browserify": "^16.2.3",
    "jquery": "^3.4.0"
  },
  "devDependencies": {
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3",
    "babelify": "^10.0.0"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
